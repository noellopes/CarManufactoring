@model IEnumerable<CarManufactoring.Models.CarConfig>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ConfigName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NumExtras)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AddedPrice)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ConfigName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NumExtras)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AddedPrice)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CarConfigId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CarConfigId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CarConfigId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
