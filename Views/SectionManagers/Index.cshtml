@model IEnumerable<CarManufactoring.Models.SectionManager>

@{
    ViewData["Title"] = "Section Manager List";
}

<h2>@ViewBag.Title</h2>

<p>
    <a asp-action="Create" class="btn btn-success mt-3">Create New</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BirthDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Genders)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Phone)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Sections)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BirthDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Genders.GenderDefinition)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Phone)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Sections.Name)
            </td>
            <td>
                    <a asp-action="Details" asp-route-id="@item.SectionManagerId" class="btn btn-info">Details</a>
                    <a asp-action="Edit" asp-route-id="@item.SectionManagerId" class="btn btn-warning">Edit</a>
                    <a asp-action="Delete" asp-route-id="@item.SectionManagerId" class="btn btn-danger">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
