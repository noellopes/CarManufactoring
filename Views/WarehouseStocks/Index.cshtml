@model IEnumerable<CarManufactoring.Models.WarehouseStock>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Identification)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Identification)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.WarehouseStockId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.WarehouseStockId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.WarehouseStockId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
