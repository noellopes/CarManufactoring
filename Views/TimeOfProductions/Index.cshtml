@model IEnumerable<CarManufactoring.Models.TimeOfProduction>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CarConfigId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Time)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CarConfigId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Time)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TimeOfProductionId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TimeOfProductionId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TimeOfProductionId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
