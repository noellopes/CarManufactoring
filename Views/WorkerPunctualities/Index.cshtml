@model IEnumerable<CarManufactoring.Models.WorkerPunctuality>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ScheduledDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MissedHoursLastWeek)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LateShiftsLastWeek)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ScheduledDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MissedHoursLastWeek)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LateShiftsLastWeek)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.WorkerPunctualityId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.WorkerPunctualityId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.WorkerPunctualityId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
