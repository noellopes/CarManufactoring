@model IEnumerable<CarManufactoring.Models.StockFinalProduct>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ChassiNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Production)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ChassiNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Production.ProductionId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.LocalizationCarId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.LocalizationCarId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.LocalizationCarId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
