@model IEnumerable<CarManufactoring.Models.CarModels>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CarName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ConfigName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CarName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ConfigName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CarModel_Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CarModel_Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CarModel_Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
