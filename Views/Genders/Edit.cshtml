@model CarManufactoring.Models.Gender

@{
    ViewData["Title"] = "Edit Gender";
}

<h2>@ViewBag.title</h2>

<h4>Gender</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="GenderId" />
            <div class="form-group">
                <label asp-for="GenderDefinition" class="control-label"></label>
                <input asp-for="GenderDefinition" class="form-control" />
                <span asp-validation-for="GenderDefinition" class="text-danger"></span>
            </div>
            <div class="form-group mt-3">
                <input type="submit" value="Save" class="btn btn-primary" />
                <a asp-action="Index" class="btn btn-secondary">Cancel</a>
            </div>
        </form>
    </div>
</div>


@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
