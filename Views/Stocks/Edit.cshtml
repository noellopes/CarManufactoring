@model CarManufactoring.Models.Stock

@{
    ViewData["Title"] = "Edit Stocks";
}

<h1>@ViewBag.Title</h1>


<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="form-text text-danger"></div>
            <input type="hidden" asp-for="StockId" />
            <div class="mt-2 mb-2">
                <label asp-for="Quantity" class="form-label"></label>
                <input asp-for="Quantity" class="form-control" />
                <span asp-validation-for="Quantity" class="form-text text-danger"></span>
            </div>
            <div class="mt-2 mb-2">
                <label asp-for="Location" class="form-label"></label>
                <input asp-for="Location" class="form-control" />
                <span asp-validation-for="Location" class="form-text text-danger"></span>
            </div>
            <div class="mt-2 mb-2">
                <label asp-for="CollaboratorId" class="form-label"></label>
                <select asp-for="CollaboratorId" class="form-control" asp-items="ViewBag.CollaboratorId"></select>
                <span asp-validation-for="CollaboratorId" class="form-text text-danger"></span>
            </div>
            <div class="mt-2 mb-2">
                <label asp-for="MaterialId" class="form-label"></label>
                <select asp-for="MaterialId" class="form-control" asp-items="ViewBag.MaterialId"></select>
                <span asp-validation-for="MaterialId" class="form-text text-danger"></span>
            </div>
            <div class="mt-2 mb-2">
                <input type="submit" value="Save" class="btn btn-primary" />
                <a asp-action="Index" class="btn btn-secondary">Cancel</a>
            </div>
        </form>
    </div>

</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
