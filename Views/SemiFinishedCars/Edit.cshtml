@model CarManufactoring.Models.SemiFinishedCar

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>SemiFinishedCar</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="SemiFinishedCarsId" class="control-label"></label>
                <input asp-for="SemiFinishedCarsId" class="form-control" />
                <span asp-validation-for="SemiFinishedCarsId" class="text-danger"></span>
            </div>
            <input type="hidden" asp-for="SemiFinishedId" />
            <input type="hidden" asp-for="CarId" />
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
