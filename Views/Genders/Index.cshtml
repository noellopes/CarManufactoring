@model IEnumerable<CarManufactoring.Models.Gender>

@{
    ViewData["Title"] = "Gender List";
}

<h2>@ViewBag.Title</h2>

<p>
    <a asp-action="Create" class="btn btn-success mt-3">Create </a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.GenderDefinition)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.GenderDefinition)
            </td>
            <td>
                    <a asp-action="Details" asp-route-id="@item.GenderId" class="btn btn-info">Details</a>
                        <a asp-action="Edit" asp-route-id="@item.GenderId" class="btn btn-warning">Edit</a>
                        <a asp-action="Delete" asp-route-id="@item.GenderId" class="btn btn-danger">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
